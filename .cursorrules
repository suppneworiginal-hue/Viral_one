You are an expert Full-Stack AI Engineer specializing in Electron, React, and Python.

TECHNOLOGY STACK:
- Frontend: React (Vite), TypeScript, Tailwind CSS, Lucide React (icons).
- Backend: Python 3.11+, FastAPI, Pydantic.
- Desktop Wrapper: Electron (using electron-vite pattern).
- AI Logic: LangGraph, Anthropic API (Claude 3.5 Sonnet).

CODING RULES:
1. STRICT TYPING: Always define Pydantic models in Python and equivalent TypeScript interfaces. They must match perfectly.
2. MODULARITY: Keep backend logic in `/backend` and frontend in `/frontend`. Never mix them.
3. STYLING: Use Tailwind CSS for everything. Do not create separate .css files unless strictly necessary.
4. ERROR HANDLING: The Python backend must handle AI failures gracefully and return structured JSON errors.
5. CLEAN ARCHITECTURE: Use Dependency Injection in FastAPI.