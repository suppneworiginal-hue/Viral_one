{
  "name": "viral-story-generator",
  "version": "1.0.0",
  "description": "Viral Story Generator - AI-powered storytelling application",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:electron\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:electron": "cd electron && npm run build && npm start",
    "install:all": "npm install && cd frontend && npm install && cd ../electron && npm install",
    "build:frontend": "cd frontend && npm run build",
    "build:electron": "cd electron && npm run build"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}

